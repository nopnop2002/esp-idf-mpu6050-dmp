## IDF Component Manager Manifest File
dependencies:
  espressif/mdns:
    version: "^1.0.3"
    rules:
      - if: "idf_version >=5.0"
  espressif/cjson:
    version: "^1.7.0"
    rules:
      - if: "idf_version >=6.0"
